<%= form_tag({}, :method => :get) do %>
  <fieldset><legend><%= l(:label_filter_plural) %></legend>
  <label><%= l(:field_server) %>:</label>
  <%= render_crontabs_jump_menu(@project,@servers,@server) %>
  <!-- <%= submit_tag l(:button_apply), :class => "small", :name => nil %> -->
  </fieldset>
<% end %>
&nbsp;
