<h2><%= link_to l(:label_datacenter_plural), datacenters_path %> &#187; <%=l(:label_datacenter_new)%></h2>

<% labelled_tabular_form_for :datacenter, @datacenter, :url => datacenters_path, :html => { :class => nil } do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
	<p><%= submit_tag l(:button_create) %></p>
<% end %>
