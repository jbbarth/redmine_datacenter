<h2><%=l(:label_datacenter_new)%></h2>

<% labelled_tabular_form_for :datacenter, @datacenter, :url => datacenter_path(@project), :html => { :class => nil } do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
	<p><%= submit_tag l(:button_create) %></p>
<% end %>
