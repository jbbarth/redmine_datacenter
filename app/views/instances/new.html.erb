<h2>
  <%= link_to l(:label_appli_plural), applis_path %> 
  &#187; <%= link_to @appli.name, appli_path(@project,@appli) %>
  &#187; <%= l(:label_instance_new) %>
</h2>

<%= labelled_form_for :instance, @instance, :url => appli_instances_path(@project,@appli),
                             :html => { :class => nil } do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
	<p><%= submit_tag l(:button_create) %></p>
<% end %>
