<h2><%= link_to l(:label_server_plural), servers_path %> &#187; <%=l(:label_server_new)%></h2>

<%= labelled_form_for :server, @server, :url => servers_path, :html => { :class => nil } do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
	<p><%= submit_tag l(:button_create) %></p>
<% end %>
